# Что нового

Записываем крупными кусками, чтобы можно было понять, что изменилось, не читая коммиты.

---

## 03.09.2026 — «Анализ дня»: несколько активов, скрины по таймфреймам, мини-календарь

Раздел молча считал, что актив один: один скрин разметки, одно направление, один
список уровней. Кто утром разбирает US100 и GER40 — второй девать было некуда.

**Активы.** Запись дня теперь — список карточек активов, одна под другой. У каждой
свои скрины, направление, уровни, сценарии. Кнопка «+ добавить актив» подсказывает
сначала инструменты из «Моей ТС» (что человек сам там записал), потом те, что уже
встречались в журнале, и поле для своего. Уже добавленные гаснут.

**Скрины по таймфреймам.** Вместо одного слота — сколько нужно: у каждого скрина чип
таймфрейма (1D, 1H, 15m…), рядом всегда пустой слот «+ таймфрейм». Вставка как везде:
клик, Ctrl+V, перетащить.

**Вечер по активам.** Карточка становится разворотом: слева план как был (только чтение),
справа факт — скрины вечера, куда пошёл рынок, что с уровнями, сделки и оценки. Сделки
из журнала ложатся к активу сами, по названию инструмента. Те, чей инструмент утром не
разбирали, попадают в блок «Другие сделки дня» — ничего не теряется. Оценки «по плану» и
«держался» — по каждому активу; для статистики они сводятся в одну оценку дня: все «да» →
«да», все «нет» → «нет», иначе «частично». Внизу — итог дня строкой на актив и «что из этого
вынести». Правила «чего не делать» остались общими на день.

**Мини-календарь.** Кнопка с датой раскрывает месяц точками: зелёная — по плану, жёлтая —
частично, красная — нет, пустой кружок — план есть, вечер не записан. Клик по дню открывает
его. Стрелки листают месяцы. Данные — `/api/day/list`, он уже был.

**Старые записи** читаются как есть: при загрузке запись без активов заворачивается в одну
карточку без названия, скрины плана и вечера переезжают в её слоты. `day_store.py` не менялся —
колонки `match_mark/hold_mark` получают сведённую оценку дня.

Заодно: сделки дня теперь ищутся по дате без времени (`slice(0,10)`), раньше запись вида
`2026-09-03T09:54` не совпадала с днём.

`static/day.js` (переписан), `static/day.css`, `__ts.assets()` в `static/ts.js`. Прототип —
`design/dayassets/`.

---

## 03.09.2026 — Помощник сверяет сделку с ТС и заговаривает сам

«Моя ТС» и журнал жили порознь. Сверка была, но статистическая: она на своей
вкладке, требует пяти сделок и молчит в тот момент, когда человек только что
нарушил собственное правило. Теперь наоборот — **одна сделка против правил,
сразу после записи**.

Проверяем то, что человек сам описал: инструмент не из своего списка, модель
входа не из своих, время вне торговых окон, день, который он помечал как
пропуск, риск выше заявленного, RR ниже минимума, дневной лимит убытка, лишний
вход сверх лимита сделок. **Пустое поле ТС молчит** — никаких «типовых»
значений вместо незаполненных. Описан только риск — проверяем только риск.

**Когда сверять не с чем — так и говорит.** Если в ТС описаны одни названия
моделей, а риск, RR, окна и лимиты пустые, правил взять неоткуда — и молчание
выглядит поломкой. Теперь помощник объясняет причину и зовёт дописать ТС. Один
раз в день, не после каждой сделки.

Считает всё Python (`ts_check.py`), модель лишь пересказывает готовые факты
одной фразой — тот же принцип, что и у остального помощника. Нет
`GEMINI_API_KEY` — помощник всё равно говорит, только сухо: «Риск 3% вместо
заявленных 1%». Показываем не больше четырёх расхождений за раз: дальше это уже
не подсказка, а укор.

**У маскота появился голос.** Реплика стоит на цвете темы — в тёмных темах это
тёмный текст на зелёном, в светлых наоборот: её должно быть видно с первого
взгляда, а не искать среди панелей. Вырастает из самого кружка — точка роста
стоит ровно в его центре, снизу у неё хвостик в его сторону, и сам он на
секунду улыбается. Появляется с коротким «дзинь» на два тона (складываем его в
браузере, файл ради 120 мс тянуть незачем; браузер не даёт звучать до первого
действия человека — тогда просто тихо). Живёт несколько секунд и уходит сама,
ничего не блокируя. Кнопка «Разобраться» переносит разговор в чат.

**И он иногда спрашивает первым** — про серию стопов, спешку между входами, не
описанную ТС, итоги недели. Приличия важнее сообразительности: не раньше чем
через минуту после захода, не чаще раза в четыре часа, не больше двух раз в
день, никогда поверх открытой формы и никогда в чужом журнале или у гостя.
Расхождение с ТС идёт вне очереди — оно про то, что человек сделал только что.

`ts_check.py`, `static/watch.js`, реплика в `assistant.js`/`assistant.css`,
`POST /api/ts/check` и `POST /api/assistant/nudge`.
Проверка: `python test_ts_check.py`.

---

## 03.09.2026 — Журнал можно открыть другим

Наружу до сих пор уходили только разовые снимки — застывшие цифры без автора.
Человек смотрел чужой день и не мог попасть к тому, кто его вёл. Теперь у
каждого есть выбор: **«Відкритий журнал»** в боковой панели. Включил — журнал
живёт по адресу `/u/<ник>`, ник берётся из аккаунта, отдельного публичного
имени заводить не нужно.

**Что видно чужому:** сделки (дата, пара, сессия, направление, модель, сетап,
результат, RR, риск, скриншоты) и вся статистика с аналитикой. **Чего не
видно:** заметки, ошибки, эмоции, «Анализ дня» и «Моя ТС» — это дневник, а не
витрина. Решает один белый список `PUBLIC_FIELDS` в `app.py`: новое поле сделки
остаётся приватным, пока его туда не впишут.

**Смотреть может только тот, у кого есть свой журнал.** Без аккаунта на
`/u/<ник>` стоит приглашение завести свой — так же, как гостя останавливают на
записи. Закрытый журнал и несуществующий ник отвечают одинаково (404): по чужому
нику нельзя даже проверить, есть ли такой человек.

Писать в чужом журнале нельзя ничем: проверка стоит в `api()`, там же, где
гостевая. Личные адреса (`/api/day`, `/api/ts`) в этом режиме не запрашиваются
вообще, а «Анализ дня» и «Моя ТС» убраны из панели и недоступны по адресу с
решёткой. Скриншоты идут через `/ushot/<ник>/<файл>` и перестают отдаваться в ту
же секунду, как журнал закрыли.

**Под снимком «поділитися»** появилась кнопка «Весь журнал @ник». Снимок теперь
помнит автора, но подпись показывается, только пока журнал открыт: закрыл — и
старые розданные ссылки перестают на него вести.

Заодно починено: кнопка «Створити журнал» в полосе гостя была невидимой. В
таблице `.go` — стрелка, которая проявляется на наведении (`opacity:0` в
`style.css`), и это же правило приходило на кнопку в полосе.

`static/pub.js`, `static/pub.css`, `static/profile.js`, колонка
`users.public_journal`, `shares.user_id`, маршруты `/u/<ник>`, `/api/u/<ник>`,
`/ushot/…` и `POST /api/me/public`.

---

## 03.09.2026 — Гостевой режим: журнал видно без аккаунта

Раньше человек по чужой ссылке «поделиться» смотрел один день, а на любой шаг
дальше упирался в страницу входа — и уходил, не поняв, что ему предлагают.
Теперь `/` без сессии отдаёт сам журнал на демонстрационных данных.

**Смотреть можно всё:** обзор, календарь, аналитику, «Мою ТС», «Анализ дня»,
новости. Сверху узкая полоса «Демо · это примеры, а не чьи-то настоящие сделки».
Чужих сделок гость не видит никогда — только тот день, которым поделились.

**Регистрация нужна только на записи.** Плата берётся не за просмотр, а за то,
чтобы что-то сохранить:

- «Новая сделка» — окно появляется до формы, а не после заполнения;
- «Создать ТС с нуля», «Подтянуть из Notion», подключение Telegram;
- любое поле в «Анализе дня»;
- ИИ-помощник.

Главная проверка стоит в `api()`: любой запрос, кроме чтения, упирается в окно
входа. Так ни одна кнопка не проскочит мимо, даже если её забудут прикрыть
отдельно. Кнопки входа берутся из `/api/auth/providers` — те же, что на странице
входа.

**В боковой панели** у гостя вместо «Выйти» появились «Создать журнал» и
«Войти». Этих кнопок на сайте не было вообще.

**Пустой журнал после регистрации** — на обзоре вместо одной кнопки три пути:
«Подтянуть из Notion» (главный), «Добавить первую сделку», «Описать свою ТС».

`static/guest.js`, `static/guest.css`, маршрут `/` в `app.py`, блок на обзоре в
`static/app.js`, призыв под снимком в `static/share.html`. Прототип пути лежит в
`design/gate/` — четыре шага, открывается двойным кликом.

Заодно починено давнее: в демо-режиме «Моя ТС» и «Анализ дня» зависали на
«Минутку…». Данные там читаются из браузера мгновенно, и перерисовка прилетала
внутрь того же `render()`, который её и вызвал, — результат затирался.

---

## 03.09.2026 — Страница «поделиться» открывается плавно

Сделка раскрывалась рывком, и страница дёргалась ещё два-три раза, пока
подгружались скриншоты: браузер не знает размер картинки, пока её не получит.
Теперь место под скриншот резервируется заранее (картинка не обрезается,
вписывается целиком), высота карточки анимируется, а сами скриншоты проявляются
мягко. Полоса прокрутки на странице скрыта — она появлялась при раскрытии
и сдвигала всю страницу вбок на свою ширину.

---

## 03.09.2026 — «Моя ТС» из Notion читается целиком

Страницу с торговой системой разбирали регулярками по ключевым словам. На своей
странице это работало, на чужой — почти нет:

- таймфреймы искали строго как `1D`, `4H`. Кто пишет `Weekly`, `Daily`, `H4`,
  `М15` — не находились вообще;
- скриншоты не ложились на строки таймфреймов по той же причине, оставались
  внизу общей кучей;
- модель входа добавлялась от простого упоминания слова: `FVG` и
  `liquidity sweep` попадали в систему человеку, который написал «в FVG не вхожу»;
- правила, управление позицией и чек-лист требовали точных слов и почти всегда
  пропускались.

Теперь страницу читает модель (`ts_ai.py`) и раскладывает по полям, а код
проверяет, что она вернула: чужие поля выкидываются, длина режется, номер
скриншота меняется на имя файла. Модели входа берутся только те, по которым
человек реально входит. Скриншот встаёт на свою строку по подписи блока в Notion.

Нет ключа или ответ не сложился — работает разбор регулярками. Он в этот же день
научился понимать любую запись таймфрейма (`H1`, `M15`, `D/4h`, `Weekly`) и
забирать описание из списка под заголовком.

Проверка: `python test_ts_notion.py`.

---

## 02.09.2026 — Телефоны, планшеты и вход через сервисы

### Мобильная версия

До 900px боковая панель прячется. Вместо неё шапка сверху (лого, «Нова угода»,
кнопка меню) и панель вкладок снизу с шестью разделами. Калькулятор, подключения,
язык, оформление и выход — в шторке по кнопке меню. Это те же самые элементы из
боковой панели, только переставленные: переводы, подсказки и обработчики свои.

Внутри разделов на узком экране всё складывается в одну колонку: показатели месяца,
календарь с панелью дня, «Моя ТС», «Аналіз дня», аналитика. Панель сделки — на весь
экран, кнопки внизу переносятся. Помощник поднят над вкладками. Планшет в книжной
ориентации ведёт себя как телефон, в альбомной — как десктоп с панелью поуже.

`static/mobile.css`, `static/mobile.js`. На широком экране ничего не меняют.

### Вход через Google, Discord и Telegram

Кнопки на странице входа раньше показывали «поки що тільки поштою». Теперь работают.
Google и Discord — обычный OAuth (`/auth/<сервис>` → сервис → `/auth/<сервис>/callback`),
Telegram — их страница входа, которая возвращает подписанные данные; подпись проверяем
ключом бота. Сервис без ключей на странице входа не показывается.

Кого уже видели — узнаём по таблице `identities`. Если пришла почта существующего
аккаунта — входим в него. Новому создаётся аккаунт со случайным паролем: заходить он
будет тем же сервисом. Вход через Telegram сразу привязывает бота.

`oauth.py`, маршруты в `app.py`, кнопки в `static/login.html`. Ключи — см. README.

**Состояние на хостинге (02.09.2026):**
- **Google** — включён и опубликован (Google Auth Platform → «In production»), заходить
  может любой аккаунт Google. Страницы `/privacy` и `/terms` нужны именно для этого.
- **Discord** — включён: приложение `StatsAI` создано в discord.com/developers, redirect
  `https://trading-journal-production-440c.up.railway.app/auth/discord/callback` добавлен,
  `DISCORD_CLIENT_ID` и `DISCORD_CLIENT_SECRET` заданы на Railway.
- **Telegram** — включён; если виджет отвечает «Bot domain invalid», в @BotFather:
  `/setdomain` → `@statsai_trader_bot` → `trading-journal-production-440c.up.railway.app`.

---

## 02.09.2026 — Раздел «Аналіз дня»

Журнал знал, **что** человек сделал, но не знал, **что он собирался** сделать. Теперь
знает: утром человек записывает план, вечером — что из этого вышло.

**Где:** пункт **«Аналіз дня»** в левой панели, сразу после «Журнала». Стрелки ← →
листают дни, кнопка «сьогодні» возвращает на текущий.

### Два вида одного дня

**Пока день идёт** — слева форма из пяти блоков: скрин утренней разметки, Long / Short /
Нейтрально с объяснением, уровни (цена · что это · зачем), два сценария, правила на день.
Справа не пустая половина, а панель «Вечір · ще попереду» со списком того, что там
появится. Кнопка «Записати підсумок дня» включается, когда план появился.

**Когда день закрыт** — разворот парами, пронумерованный с обеих сторон: `01` слева
всегда отвечает `01` справа. Утро / вечер, строка против строки.

Всё правится на месте: клик по значению, `Enter` сохраняет, `Esc` отменяет. Пишется
само, кнопки «сохранить» нет.

### Что не надо вводить руками

- **Сделки дня** подтягиваются из журнала по дате. Рядом с каждой — переключатель
  «за планом / поза планом»; это и есть главные данные раздела.
- **Новости** берутся из раздела «Новини».

### Полоса за месяц

Считается из базы по последним 30 дням: сколько дней сценарий сыграл, сколько сделок
было вне плана и сколько они в сумме стоили. Ради этой цифры раздел и нужен — одна
сделка «сверх плана» обычно съедает половину дневного плюса.

### Поделиться одной сделкой

У каждой сделки в разборе дня — своя кнопка (появляется при наведении на строку).
Открывает то же окно, что и в журнале: ссылка или картинка **только по этой сделке**,
со всеми деталями входа и скринами. Весь день отправлять для этого не нужно.

Заодно починена сама картинка сделки: высота считалась приблизительно, и на сделках
с заметками подпись «зроблено в StatsAI» налезала на последнюю строку. Теперь высота
считается ровно по тому, что рисуется.

### Где что лежит

| Файл | Что |
|---|---|
| `static/day.js` | весь раздел: два вида, правка на месте, скрины, подсчёт полосы |
| `static/day.css` | стили, все под префиксом `.dv-` |
| `day_store.py` | таблица `day_notes` (документ JSON на день + две оценки колонками), скрины |

Таблица: `day_notes (user_id, date, data, match_mark, hold_mark)`. Оценки «рынок пошёл
по плану» и «держался плана» вынесены в колонки — по ним считается статистика, и лезть
в JSON каждый раз не хочется. Скрины лежат в `data/screenshots/` с именем
`dn<id пользователя>_...`, отдаются только владельцу.

В `app.py` только маршруты (`/api/day/<дата>`, `/api/day/list`, `/api/day/stats`,
`/api/day/shot`, `/dnshot/<файл>`), в `static/index.html` — пункт меню и два подключения.
Словарь раздела (uk/ru/en) пока внизу `static/day.js`, как и у «Моей ТС».

Пробник, по которому это собрано, остался в `design/dayview/` — там же два других
варианта (лента по часам и график с ползунком «утро / вечер»).

---

## 01.09.2026 — Раздел «Моя ТС»

Журнал считал статистику, но не знал, по каким правилам человек торгует. Теперь знает —
и сверяет с ними каждую сделку.

**Где:** пункт **«Моя ТС»** в левой панели, между «Аналітикой» и «Новинами».

### Пустой экран

Пока стратегии нет, человек видит два пути:

- **Создать ТС с нуля** — 18 вопросов по одному на экран (чем торгуешь, в какие окна,
  где входишь, сколько рискуешь, когда переводишь в безубыток). Почти всё — кнопками.
- **Подтянуть из Notion** — поле под ссылку. Ключей и доступов не надо, как и в переносе
  сделок: страница должна быть `Share → Publish`.

### Готовая ТС

Восемь блоков: рынок и время, таймфреймы, вход, риск, сопровождение, когда не вхожу,
чек-лист и сверка с журналом.

**Правится на месте.** Любое значение — клик по нему, `Enter` сохраняет, `Esc` отменяет.
Списки (окна, модели, правила, пункты чек-листа) добавляются и убираются кнопками.
Ничего сохранять руками не нужно: пишется само.

**Скрины.** Слот стоит у каждого таймфрейма, у каждой модели входа, у стопа, цели и у
каждого правила сопровождения. Три способа положить: клик (выбрать файл), навести и
`Ctrl+V`, перетащить. Картинка ужимается до 1100 px и кладётся в `data/screenshots/`
с именем `ts<id пользователя>_...` — по имени видно владельца, чужой скрин не отдаётся.

**Сверка с журналом** (блок «Що виходить насправді»). Считается из реальных сделок:
сколько раз RR был ниже заявленного минимума, в скольких днях пробит дневной лимит и
какой был худший, где сделок за день больше, чем в правилах, держится ли риск, сколько
входов сделано по своим моделям. Пустые поля ТС просто не участвуют — ничего не
додумываем.

### Чтение ТС из Notion

`ts_notion.py`. Страница с ТС — не таблица, а текст, поэтому разбор свой: строки
раскладываются на таймфреймы, инструменты, модели, окна сессий, проценты риска и лимитов,
минимальный RR. Что не распозналось — остаётся в блоке «Страница из Notion, как мы её
прочитали», рядом со скринами со страницы. Если число на странице не нашлось, поле
остаётся пустым, а не заполняется «типовым» значением.

### Где что лежит

| Файл | Что |
|---|---|
| `static/ts.js` | весь раздел: экраны, опрос, правка на месте, слоты под скрины, сверка |
| `static/ts.css` | стили, все под префиксом `.ts-` |
| `ts_store.py` | таблица `strategies` (документ JSON на пользователя), скрины и уборка за ними |
| `ts_notion.py` | чтение и разбор страницы Notion |

В `app.py` только маршруты (`/api/ts`, `/api/ts/shot`, `/api/ts/notion`, `/api/ts/clear`,
`/tsshot/<файл>`), в `static/index.html` — пункт меню и два подключения файлов.

В публичном демо (когда сервера нет) раздел работает так же, только ТС лежит в
браузере, а скрин остаётся картинкой внутри самого документа — класть его некуда.

Словарь раздела (uk/ru/en) пока лежит внизу `static/ts.js`, а не в `i18n.js`: раздел
ещё шевелится, так его правки не задевают общий файл. Переезд — когда устоится.

---

## 31.08.2026 — Notion, темы, калькулятор, ссылки

Большой заход. Ниже по темам: что появилось, зачем и что важно знать при работе с кодом.

### Перенос журнала из Notion

Главное за этот день. Человек уже год ведёт журнал в Notion — руками он сюда двести
сделок не перенесёт, значит и пользоваться не станет. Теперь переносим за него:
сделки, заметки из тела карточек и скриншоты.

**Открыть:** кнопка **«Підключити Notion»** в левой панели. Плюс окно один раз само
всплывает после входа.

**Никаких ключей.** Человек делает два шага: в Notion `Share → Publish to web`, потом
`Copy web link` и вставляет ссылку. Официальное API требует «Internal Integration
Secret» — три шага, на каждом теряется половина людей. Поэтому читаем страницу тем же
способом, каким её читает сайт Notion для случайного посетителя: `loadPageChunk`
и `queryCollection` без авторизации.

Это внутренний интерфейс Notion, в документации его нет. Он может измениться — но
ломается заметно: просто перестанет читать, молча неверных данных не даст.

**Колонки узнаём в два прохода.** Сначала по названию (словарь на трёх языках), потом
по самим значениям: если в колонке лежат Win/Loss/BE — это результат, как бы она ни
называлась. Long/Short — направление, LONDON/NY — сессия, US100 — инструмент.
Тот же разбор проверяет догадки по названию: колонка «Час», внутри которой LONDON,
не дата.

**Журнал по месяцам.** Частая раскладка: таблица-оглавление, в ней строки
«Trading Journal (August 2026)», а сделки внутри этих страниц. Такое оглавление
узнаётся, и мы заходим внутрь строк. Нашлось несколько таблиц — показываем список
с галочками, можно перенести все месяцы за раз.

**Откат.** Каждый перенос помечается. Кнопка «Скасувати перенесення» убирает ровно его
сделки и скриншоты — и сразу после переноса, и через день. Без этого любая ошибка
в сверке колонок была необратима.

Подробности — в [NOTION.md](NOTION.md).

### Раздел «Новини»

Экономический календарь на неделю с разбивкой по важности. Данные — недельный фид
Forex Factory. В сеть ходит только сервер, раз в полчаса: фид блокирует при частых
запросах, и сто открытых вкладок заблокировали бы всех.

Каждая удачная выгрузка дописывается в `data/calendar/`. **Архив важнее, чем кажется:**
фид истории не отдаёт, взять её больше негде, и каждый день без запущенного сервера
потерян навсегда.

### Ссылки «поділитися»

Кнопки над «Оглядом» и «Журналом» (день · тиждень · місяць · рік), отдельная кнопка
в панели дня и в карточке сделки. Срок жизни ссылки выбирается: от часа до без
ограничения.

По ссылке уходят **только посчитанные цифры**, не сами сделки: вытащить журнал
по ссылке нельзя.

Кнопка появляется только если в этом периоде есть сделки — пустой день отправлять
незачем.

### Темы оформления

Девять готовых наборов — пять тёмных (Ніч, Графіт, Опівніч, Вино, Хвоя) и четыре
светлых (День, Папір, Туман, Пісок) — плюс своя, собранная из двух цветов: фона
и акцента.

Каждая тема задаёт всего 13 цветов, остальные два десятка выводятся из них один раз
через `color-mix`. Поэтому темы короткие, а вид согласованный: не бывает так, что фон
поменялся, а плашка осталась от старой темы.

Кнопка **«Оформлення»** внизу левой панели. **Прежний переключатель светлая/тёмная
убран** — это теперь часть выбора темы.

### Калькулятор риска

```
лоти = (депозит × ризик%) / (дистанція до стопу × ціна пункту)
```

Восемь инструментов в пресетах плюс своё. Дистанция считается из цен входа и стопа,
направление определяется само. Лоты округляются **вниз** — чтобы не перебрать риск, —
и показывается, сколько потеряется на самом деле. Плюс цена тейка по плановому RR.

Цена пункта у брокеров разная, поэтому она не зашита: подставляем обычную, её можно
исправить, и она запоминается для этого инструмента.

Проверено на эталонном примере: 10 000, риск 2%, стоп 35 пунктов, цена пункта 10 →
0.57 лота.

### Экран входа

Отдельная страница `/login` с анимацией свечей и параллаксом. Пока это не настоящий
вход: отметка живёт в браузере, аккаунтов и сервера авторизации ещё нет. Кнопка
«Вийти» в меню её стирает.

### График эквити — переделан

Был рваный: день без сделок тянулся полкой, шесть сделок за один день падали отвесной
стенкой, подписи осей не совпадали с линиями сетки.

Что сделано:

- **Шаг по горизонтали — одна сделка, а не один день.** Так же, как в личном журнале
  владельца. Плата: месяц с сорока сделками занимает больше ширины, чем с пятью.
- **Засечки круглые** — шаг из ряда 1 / 2 / 2.5 / 5 / 10, линии сетки рисуются ровно
  по подписям. Раньше выходило 3, 0, −3, −5, −8.
- **Подписи по X стоят над своими точками**, а не делят ширину на равные доли.
- **Линия плавная** — монотонная кубика: проходит ровно по точкам и не выскакивает
  за них. На графике денег это важно, иначе рисовались бы прибыли, которых не было.
- **Мягкая заливка градиентом** под линией, своя для плюса и минуса.

### Карточка сделки — переделана

В шапке уже стоят инструмент, направление, время и итог в процентах. Ниже всё это
повторялось ещё раз — отсюда и каша. Повторы убраны, плиток три вместо четырёх.

Незаполненные поля больше не занимают по строке каждое: собираются в одну приглушённую
строку «не заповнено: Помилки, Коментарі».

Раздел «Нотатки» переименован в «Що записав» — внутри него было поле с таким же
названием.

### Мелочи

- Статика отдаётся с `Cache-Control: no-store`. Без этого браузер показывал старую
  версию js после правок — правки будто не применялись.
- Убрана легенда под календарём и сетка квадратиков на фоне.
- Левая панель: вместо «Імпорт / Експорт / Notion» одна понятная кнопка. Загрузка
  из файла никуда не делась — она вкладкой в том же окне. Сохранение копии журнала
  переехало туда, где нужно: на шаг сверки колонок, перед самим переносом.

---

## Разделение файлов

До этого дня правки шли по зонам без пересечений: `app.py` и новые файлы — одна
сторона, `static/app.js` и `static/style.css` — другая. За этот день **зона нарушена**,
и об этом надо знать:

| Файл | Что менялось |
|---|---|
| `static/app.js` | переписана `tradeBodyHtml` (карточка сделки), переработан `plChart` / `plPath` (график эквити), убран блок `.callegend` |
| `static/style.css` | правила осей графика, `.tstats` на три колонки, убран `.bg-grid`, добавлены стили знака в сайдбаре |
| `static/index.html` | подключение новых файлов, кнопки левой панели, вход, тема до отрисовки |

Всё остальное — новые файлы, они ничьей работе не мешают:

```
notion_public.py      чтение Notion по публичной ссылке
notion_import.py      разбор колонок и значений, загрузка картинок
static/notion.js      окно переноса
static/import.css     его стили
static/news.js        раздел «Новини»
static/news.css       его стили
static/sharelink.js   ссылки «поділитися»
static/share.html     страница снимка по ссылке
static/themes.js      выбор темы
static/themes.css     сами темы
static/calc.js        калькулятор риска
static/calc.css       его стили
static/login.html     экран входа
```

## Что не сделано

- **Русский и английский в самом журнале.** Тексты вшиты в `static/app.js` прямо
  в разметке. Работа большая и заденет чужой файл целиком — делать надо, договорившись.
- **Приватные базы Notion.** Работает только опубликованное в веб.
- **Обратная синхронизация с Notion.** Переносим в одну сторону.
- **Тесты на расчёты.** Их нет до сих пор, а логика подсчёта переписывалась.
